# Server
server.port=8080

# Database (Supabase PostgreSQL)
spring.datasource.url=jdbc:postgresql://db.fobdyhgaeyrukcsmraur.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=SchoolSystem@0504
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=EduManageProSecretKey2024VeryLongAndSecureJWTSigningKey!
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=http://localhost:3000

# Logging
logging.level.com.schoolerp=DEBUG

